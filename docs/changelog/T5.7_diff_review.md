# T5.7 — Diff для review

**PR #241** · гілка `refactor/t5-7-remove-router-v2`  
**База:** `main` (містить T5.1–T5.6)  
**Загалом:** 78 додано / 1 132 видалено — 9 файлів

---

## Видалені файли

### `app/ai_router.py` — −523 рядки

Весь cognitive router:

```
- ROUTER_SYSTEM_PROMPT (~150 рядків)
- ROUTER_TOOL definition
- cognitive_route_message() — LLM call → {"target_agent", "confidence", "intent_bucket"}
- _format_short_history() helper
```

Router робив одне: вирішував який агент обробить повідомлення. Після T5.6 залишився єдиний LLM — `coach_agent`. Рішення приймати нікому і нема для кого — файл видалено повністю.

### `tests/test_router_new.py` — −151 рядок

Єдиний тест імпортував `app.ai_router` → `ImportError` після видалення. Видалено.

---

## Змінені файли

### `app/orchestrator.py` — −196 рядків (+78)

**Видалені імпорти:**
```diff
-from app.ai_router import cognitive_route_message
-from app.logging.router_logging import log_metric, log_router_decision
+from app.logging.router_logging import log_metric
-    mock_manager_agent,
```

**Видалена `call_router()` функція (~25 рядків):**
```diff
-async def call_router(user_id, message_text, context=None) -> Dict:
-    context_payload = context or await build_user_context(...)
-    router_input = {user_id, current_state, latest_user_message, short_term_history}
-    router_output = await cognitive_route_message(router_input)
-    return {router_result, router_meta, fsm_state, context_payload, ...}
```

**Замінений `log_router_decision` → `log_metric`** у 2 місцях:
```diff
-    log_router_decision({"event_type": "fsm_transition", "agent": target_agent, ...})
+    log_metric("fsm_transition", extra={"agent": agent, ...})

-    log_router_decision({"event_type": "plan_snapshot", ...})
+    log_metric("plan_snapshot", extra={...})
```

**Перейменований параметр `target_agent` → `agent`** у `_guard_fsm_transition()` і `_commit_fsm_transition()` — прибирає routing vocabulary з внутрішнього API:
```diff
-def _guard_fsm_transition(current_state, transition_signal, target_agent, ...):
+def _guard_fsm_transition(current_state, transition_signal, agent, ...):
```

**Новий `handle_incoming_message` flow** — замінює router + dispatch на пряме:

```diff
-    router_output = await call_router(user_id, message_text, context_payload)
-    router_result = router_output.get("router_result", {})
-    target_agent = router_result.get("target_agent", "coach")
-    ...
-    if target_agent == "plan": ...
-    elif target_agent == "coach": ...
-    elif target_agent == "manager": ...

+    # Onboarding path — state-based, not routing
+    if current_state == "IDLE_NEW" or current_state.startswith("ONBOARDING:"):
+        onboarding_result = await mock_onboarding_agent(payload)
+        return await _finalize_reply(...)
+
+    # All live-user states → coach_agent directly
+    worker_result = await coach_agent(coach_payload)
```

**Видалена `_invoke_agent()` функція** (~15 рядків) — dispatch switch по `target_agent`. Більше не потрібна.

---

### `app/config.py` — −13 рядків

```diff
-    ROUTER_MODEL: str = "gpt-5-mini"
-    LEGACY_PLAN_FLOW_ENABLED: bool = False
-    self.ROUTER_MODEL = os.getenv("ROUTER_MODEL", "gpt-5-mini")
-    self.LEGACY_PLAN_FLOW_ENABLED = False
-ROUTER_MODEL = settings.ROUTER_MODEL
-LEGACY_PLAN_FLOW_ENABLED = settings.LEGACY_PLAN_FLOW_ENABLED
```

Обидві константи мертві: router видалено, PLAN_FLOW видалено в T5.3/T5.5.

---

### `app/logging/router_logging.py` — −1/+1

`log_router_decision` перейменований на `_emit` (приватна функція). Публічний API — тільки `log_metric`. Назва `log_router_decision` більше не відповідала реальності після видалення router.

---

### `app/workers/mock_workers.py` — −11 рядків

```diff
-async def mock_manager_agent(payload): ...
-    "mock_manager_agent": mock_manager_agent,
```

Єдиний caller — видалений `_invoke_agent`. Orphaned після T5.7.

---

### `tests/test_orchestrator.py` — −229 рядків

Видалено 4 async тести що монкіпатчили `call_router` і `_invoke_agent`:
- `test_non_coach_agent_returns_immediately`
- `test_coach_agent_allows_single_reroute`
- `test_coach_early_return_receives_completion_context_in_idle_finished`
- `test_plan_in_idle_new_returns_onboarding_guard` / `test_plan_tool_call_invokes_handler`

**Залишились** 8 pure unit тестів: `get_avg_difficulty_*`, `auto_complete_*` — не торкаються routing.

```python
# NOTE: coach integration tests deferred to T5.8
# (coach prompt + tool registration not yet implemented)
```

---

## Архітектура після T5.7

**До:**
```
Telegram → Orchestrator → call_router() → cognitive_route_message() [LLM]
                              ↓ target_agent
                          _invoke_agent()
                              ↓
                     coach / plan / manager / onboarding
```

**Після:**
```
Telegram → Orchestrator → IDLE_NEW/ONBOARDING:* → mock_onboarding_agent
                              ↓ (всі інші стани)
                          coach_agent (direct)
```

Один LLM. FSM стан визначає контекст, не routing.

---

## Тести

```
tests/test_orchestrator.py         — 8 passed   ✅  (unit only, routing removed)
tests/test_fsm_states.py           — 8 passed   ✅
tests/test_plan_runtime_tools.py   — 14 passed  ✅
tests/test_plan_pause_v5.py        — 7 passed   ✅  (ізольовано)
tests/test_plan_builder_v5.py      — 8 passed   ✅
tests/test_plan_completion_metrics — 8 passed   ✅
```

---

## Reviewer grep

```
rg "call_router|cognitive_route_message|target_agent|log_router_decision|ai_router|ROUTER_MODEL|LEGACY_PLAN_FLOW" app/ | grep -v ".pyc"
```

**0 matches** ✅

---

## Стратегічне питання перед мержем

> TBD — обговорити окремо.

---

## Що залишилось (T5.8)

| Завдання | Пріоритет |
|----------|-----------|
| Coach prompt: зареєструвати `plan_runtime` tools, прибрати згадки PLAN_FLOW/adaptations | до деплою |
| Інтеграційні тести orchestrator ↔ coach після готовності промпту | T5.8 |
| `ADAPTATIONS_ENABLED` з `config.py` — аналогічний шум | backlog |
| `had_adaptations`/`adaptation_count` з `CompletionMetrics` | backlog |
