# T5.6 — Diff для review

**PR #238** · гілка `refactor/t5-6-plan-runtime-tools`  
**База:** T5.5 (`refactor/t5-5-plan-flow-cleanup`)  
**Загалом:** 711 додано / 805 видалено — 5 файлів

---

## Видалений файл

### `app/ai_plans.py` — −780 рядків

Весь файл. Містив:

| Функція / блок | Рядків | Статус до видалення |
|---|---|---|
| `_PLAN_FLOW_ENTRY_PROMPT` + `_PLAN_FLOW_ENTRY_TOOL` | ~90 | broken — повертав неіснуючий `PLAN_FLOW:DATA_COLLECTION` сигнал |
| `_PLAN_FLOW_DATA_COLLECTION_PROMPT` + tool | ~80 | мертвий — стан видалено в T5.3 |
| `_PLAN_FLOW_CONFIRMATION_PENDING_PROMPT` + tool | ~90 | мертвий — стан видалено в T5.3 |
| `_SCHEDULE_ADJUSTMENT_*_PROMPT` × 3 + tools × 3 | ~150 | жива логіка, тепер у `plan_runtime/tools.py` |
| `plan_agent()` dispatcher | ~20 | call site видалено з orchestrator |
| `plan_flow_entry()` | ~45 | broken, замінено tools |
| `plan_flow_data_collection()` | ~45 | мертвий |
| `plan_flow_confirmation_pending()` | ~55 | мертвий |
| `schedule_adjustment()` | ~60 | жива — замінена `change_day_time` / `change_evening_time` |

---

## Змінений файл

### `app/orchestrator.py` — −32 рядки (+4)

#### Видалений імпорт

```diff
-from app.ai_plans import PlanAgentEnvelopeError, plan_agent
```

`PlanAgentEnvelopeError` все ще використовується в `_persist_generated_plan()` → визначений локально:

```python
class PlanAgentEnvelopeError(ValueError):
    """Raised when a generated plan payload is structurally invalid."""
```

#### Видалений dispatch в `_invoke_agent()`

```diff
-    if target_agent == "plan":
-        try:
-            return await plan_agent(payload)
-        except Exception as exc:
-            log_router_decision({...})
-            logger.error("[PLAN_AGENT] Error during plan agent call...")
-            return _plan_agent_fallback_envelope()
+    # "plan" routing is now handled by coach_agent (T5.6 — plan_agent removed).
     return await coach_agent(payload)
```

#### Спрощений `target_agent == "plan"` guard

```diff
-    if target_agent == "plan" and current_state not in (ENTRY_PROMPT_ALLOWED_STATES | {SCHEDULE_ADJUSTMENT}):
+    if target_agent == "plan":
+        # T5.6: plan_agent removed. All "plan" routing goes to coach_agent.
         if current_state == "IDLE_NEW":
             return await _finalize_reply("Спочатку пройди вітальний процес...")
-        logger.warning("Plan agent invoked from forbidden state %s...", ...)
+        logger.info("Plan routing redirected to coach for user %s (state=%s)", ...)
         coach_result = await _invoke_agent("coach", {...})
         return await _finalize_reply(str(coach_result.get("reply_text") or ""))
```

Тепер **будь-який** `target_agent == "plan"` → coach, без винятків по стану. `IDLE_NEW` guard залишений (онбординг ще не завершено).

---

## Новий файл

### `app/plan_runtime/__init__.py` — порожній

### `app/plan_runtime/tools.py` — +364 рядки

8 чистих функцій. Всі DB-імпорти lazy (як у `plan_pause.py`) — тестуються без реальної БД.

#### `create_first_plan(user_id) → dict`

```python
# Інваріанти:
# - user.current_state == "IDLE_ONBOARDED" → else ValueError
# - profile.daily_time_slots["DAY"] встановлено → else ValueError
# → create_plan(db, user_id, "SHORT", day_time, evening_time=None)
# → user.current_state = "ACTIVE"
# → return {"status": "ok", "plan_type": "SHORT"}
```

#### `create_followup_plan(user_id, plan_type) → dict`

```python
# plan_type: "SHORT" | "MEDIUM" → else ValueError
# user.current_state in {IDLE_FINISHED, IDLE_DROPPED, IDLE_PLAN_ABORTED} → else ValueError
# MEDIUM без evening_slot_collected → return {"status": "needs_evening_time"}  # не raise
# → create_plan(...) → user.current_state = "ACTIVE"
```

#### `record_evening_time(user_id, hhmm) → dict`

```python
# hhmm формат HH:MM (^\d{2}:\d{2}$) → else ValueError
# → profile.daily_time_slots["EVENING"] = hhmm
# → profile.evening_slot_collected = True
```

#### `change_day_time(user_id, hhmm) → dict`

```python
# → profile.daily_time_slots["DAY"] = hhmm
# → reschedule_plan_steps(future pending/delivered DAY-slot step IDs)
# → return {"status": "ok", "day_time": hhmm, "rescheduled": N}
```

#### `change_evening_time(user_id, hhmm) → dict`

```python
# Аналогічно change_day_time але для EVENING-slot кроків
```

#### `get_plan_status(user_id) → dict`

```python
# → {"state": current_state, "plan_active": bool, "days_total": N, "days_completed": N}
```

#### `pause_plan(user_id) → dict`

```python
# user.current_state == "ACTIVE" → else ValueError
# → delegates to app.plan_pause.pause_plan()
# → profile.is_paused=True, pause_count+=1, current_state="ACTIVE_PAUSED"
```

#### `resume_plan(user_id) → dict`

```python
# user.current_state == "ACTIVE_PAUSED" → else ValueError
# → delegates to app.plan_pause.resume_plan()
# → profile.is_paused=False, current_state="ACTIVE"
```

---

## Нові тести

### `tests/test_plan_runtime_tools.py` — +336 рядків, 12 тестів

```
test_create_first_plan_happy_path           IDLE_ONBOARDED + day_time → ACTIVE, SHORT ✅
test_create_first_plan_wrong_state          IDLE_NEW → ValueError ✅
test_create_first_plan_no_day_time          IDLE_ONBOARDED, day_time=None → ValueError ✅
test_create_followup_plan_short             IDLE_FINISHED → ACTIVE, SHORT ✅
test_create_followup_plan_medium_no_evening IDLE_FINISHED, evening_collected=False → needs_evening_time ✅
test_create_followup_plan_medium_ok         IDLE_FINISHED, evening_collected=True → ACTIVE, MEDIUM ✅
test_record_evening_time                    → EVENING slot + evening_slot_collected=True ✅
test_record_evening_time_bad_format         "9:00" → ValueError ✅
test_change_day_time_reschedules            → rescheduled > 0 ✅
test_pause_plan                             ACTIVE → ACTIVE_PAUSED ✅
test_resume_plan                            ACTIVE_PAUSED → ACTIVE ✅
test_pause_wrong_state                      ACTIVE_PAUSED → ValueError ✅
```

---

## Тести

```
tests/test_plan_runtime_tools.py      — 12 passed  ✅  (новий)
tests/test_fsm_states.py              —  8 passed  ✅
tests/test_plan_completion_metrics.py —  8 passed  ✅
tests/test_plan_pause_v5.py           —  9 passed  ✅  (не зачеплено)
tests/test_plan_builder_v5.py         —  8 passed  ✅
Total: 45 passed in 1.32s
```

---

## Reviewer grep: результати

```
rg "plan_agent|from app.ai_plans|import ai_plans|PLAN_FLOW" app/ | grep -v ".pyc"
```

| Match | Файл | Тип |
|---|---|---|
| `LEGACY_PLAN_FLOW_ENABLED` | `app/config.py` | frozen config constant, не runtime |
| `PLAN_FLOW` в коментарях | `app/ai_router.py` | документація, не код |

**Нуль runtime callers. Нуль plan_agent викликів.**

---

## Що залишилось (наступні PR)

| Завдання | Файли | Пріоритет |
|----------|-------|-----------|
| **T5.7** — Coach prompt: зареєструвати plan_runtime tools, прибрати router | `app/workers/coach_agent.py`, `app/ai_router.py` | до деплою |
| `cancel_plan()` — FSM `ACTIVE → IDLE_PLAN_ABORTED` + `cancel_plan_step_jobs` | новий `app/plan_runtime/tools.py` | до деплою |
| Видалити `plan_parameters` / `known_parameters` | `app/session_memory.py`, `app/orchestrator.py` | після T5.7 |
| Видалити `LEGACY_PLAN_FLOW_ENABLED` з `config.py` | `app/config.py` | backlog |
| Видалити `had_adaptations`/`adaptation_count` з `CompletionMetrics` | `app/plan_completion/metrics.py` | backlog |
| **T5.8 тести** — написати інтеграційні тести orchestrator ↔ coach після реалізації coach prompt і tool registration. Зараз 4 async тести видалено з `test_orchestrator.py` (були під router який більше не існує). Потрібні нові тести: coach отримує правильний контекст по стану, tool calls обробляються, FSM transition після tool відповіді. | `tests/test_orchestrator.py` | T5.8 |
