# T5.4 — Diff для review

**PR #235** · гілка `refactor/t5-4-delete-adaptation`  
**База:** T5.3 (`refactor/t5-3-fsm-cleanup`)  
**Загалом:** 1972 додано / 7314 видалено по 51 файлах

---

## Видалені файли (3 006 рядків)

| Файл | Рядків |
|------|--------|
| `app/adaptation_executor.py` | 914 |
| `app/adaptation_types.py` | 333 |
| `app/adaptation_metrics.py` | 177 |
| `app/ux/adaptation_preview.py` | 224 |
| `app/logic/rule_engine.py` | 37 |
| `tests/test_adaptation_duration.py` | 699 |
| `tests/test_adaptation_load.py` | 333 |
| `tests/test_adaptation_timeout.py` | 299 |
| `tests/test_adaptation_pause_resume.py` | 121 |
| `tests/test_canonical_plan_duration.py` | 62 |
| `tests/test_session_memory_adaptation_timeout.py` | 56 |
| `tests/test_telemetry_dispatch.py` | 44 |

---

## Змінені файли

### `app/orchestrator.py` — −682 рядки

**Видалені імпорти:**

```diff
-from app.adaptation_executor import AdaptationExecutor, AdaptationResult
-from app.adaptation_types import (
-    AdaptationIntent, ADAPTATION_FLOW_ENTRY_STATES, ...
-)
-from app.fsm.states import PLAN_FLOW_ALLOWED_TRANSITIONS
```

**Видалені функції (весь adaptation flow):**

```diff
-async def compute_available_adaptations(...)
-async def build_adaptation_payload(...)
-async def check_adaptation_eligibility(...)
-async def handle_undo_last_adaptation(...)
-async def execute_adaptation(...)
-async def handle_adaptation_response(...)
-async def handle_adaptation_flow(...)
```

**Видалений FSM guard-блок:**

```diff
-    if from_state in {"ACTIVE", "ACTIVE_PAUSED", "ACTIVE_PAUSED_CONFIRMATION"}:
-        ...
-    if to_state == "ADAPTATION_SELECTION" and from_state in ADAPTATION_FLOW_ENTRY_STATES:
-        ...
-    if to_state.startswith("ADAPTATION_") and _is_adaptation_tunnel(from_state, to_state):
-        ...
-    # PLAN_FLOW:FINALIZATION → ACTIVE (removed with PLAN_FLOW deletion)
```

**Залишилось** (не T5.4-scope): `apply_plan_adaptation` для pause/resume на рядках 168, 1957 — це execution-level обробник, не adaptation flow.

---

### `app/ai_plans.py` — −572 рядки

**Видалені імпорти:**

```diff
-from app.fsm.states import ADAPTATION_CONFIRMATION, ADAPTATION_PARAMS, ADAPTATION_SELECTION
-from app.adaptation_types import get_all_intent_values, get_intents_requiring_params
```

**Видалені константи (3 промпти + 3 tool definitions):**

```diff
-_ADAPTATION_FLOW_SELECTION_PROMPT = "..."
-_ADAPTATION_FLOW_PARAMS_PROMPT = "..."
-_ADAPTATION_FLOW_CONFIRMATION_PROMPT = "..."
-_ADAPTATION_FLOW_SELECTION_TOOL = {...}
-_ADAPTATION_FLOW_PARAMS_TOOL = {...}
-_ADAPTATION_FLOW_CONFIRMATION_TOOL = {...}
```

**Видалені dispatch гілки в `plan_agent()`:**

```diff
-    if current_state == ADAPTATION_SELECTION:
-        return await adaptation_flow_selection(...)
-    if current_state == ADAPTATION_PARAMS:
-        return await adaptation_flow_params(...)
-    if current_state == ADAPTATION_CONFIRMATION:
-        return await adaptation_flow_confirmation(...)
```

**Видалені функції:**

```diff
-async def adaptation_flow_selection(...)
-async def adaptation_flow_params(...)
-async def adaptation_flow_confirmation(...)
```

**Видалено з entry prompt:**

```diff
-## B) MODIFY existing plan
-You can request plan adaptations: reduce load, shift timing, etc.
-Use transition_signal: ADAPTATION_SELECTION to enter adaptation flow.
```

---

### `app/scheduler.py` — −88 рядки

```diff
-ADAPTATION_SOFT_TIMEOUT_MIN = 30
-ADAPTATION_HARD_TIMEOUT_MIN = 60

-async def _send_adaptation_timeout_prompt(user_id: int) -> None: ...
-async def _force_reset_adaptation(user_id: int, db: Session) -> None: ...
-async def check_stuck_adaptations() -> None: ...

-scheduler.add_job(
-    check_stuck_adaptations,
-    trigger=IntervalTrigger(minutes=5),
-    id="check_stuck_adaptations",
-)
```

---

### `app/telegram.py` — −81 рядок

```diff
-_ADAPTATION_ACTIONS = [
-    "reduce_load", "shift_timing", "pause", "resume", "undo"
-]
-_ADAPTATION_TIMEOUT_CALLBACKS = [f"adapt_timeout_{a}" for a in _ADAPTATION_ACTIONS]

-def _build_adaptation_action_keyboard() -> InlineKeyboardMarkup: ...
-def _adaptation_action_text(action: str) -> str: ...

-@router.callback_query(F.data.in_(_ADAPTATION_ACTIONS))
-async def on_adaptation_action(callback: CallbackQuery, ...): ...

-@router.callback_query(F.data.in_(_ADAPTATION_TIMEOUT_CALLBACKS))
-async def on_adaptation_timeout_action(callback: CallbackQuery, ...): ...

-elif _user_state == "ADAPTATION_CONFIRMATION":
-    reply_markup = _build_adaptation_action_keyboard()  # (×2)
```

---

### `app/session_memory.py` — −138 рядків

Видалені key helpers і методи:

```diff
-def _adaptation_context_key(self, user_id: int) -> str: ...
-def _adaptation_last_active_key(self, user_id: int) -> str: ...
-def _adaptation_soft_prompted_key(self, user_id: int) -> str: ...

-async def get_adaptation_context(...)
-async def set_adaptation_context(...)
-async def update_adaptation_context(...)
-async def clear_adaptation_context(...)
-async def set_adaptation_last_active(...)
-async def get_adaptation_last_active(...)
-async def clear_adaptation_last_active(...)
-async def set_adaptation_soft_prompted(...)
-async def get_adaptation_soft_prompted(...)
-async def clear_adaptation_soft_prompted(...)
```

---

### `app/plan_completion/metrics.py` — рефакторинг

**Видалено** мертвий імпорт:

```diff
-from app.plan_metrics import fetch_delivered_steps
```

**Hardcode adaptation_count = 0 (T5.4):**

```diff
-    adaptation_count = get_adaptation_count(db, plan_id)
+    # T5.4: adaptations removed — always 0. Remove field from CompletionMetrics when
+    # completion report is redesigned (backlog).
+    adaptation_count = 0
```

**Extracted `_fetch_eligible_steps`** — виділено для testability:

```python
def _fetch_eligible_steps(
    db: Session,
    plan_id: int,
    now_utc: datetime,
) -> list[AIPlanStep]:
    return (
        db.query(AIPlanStep)
        .join(AIPlanDay, AIPlanDay.id == AIPlanStep.day_id)
        .filter(
            AIPlanDay.plan_id == plan_id,
            AIPlanStep.step_status.in_(["completed", "skipped", "expired"]),
            AIPlanStep.scheduled_for <= now_utc,
            AIPlanStep.canceled_by_adaptation == False,
        )
        .all()
    )
```

---

### `tests/test_orchestrator.py` — −170 рядків

Видалені 3 тести:

```diff
-async def test_adaptation_tunnel_plan_handled_before_plan_agent(...)
-async def test_adaptation_tunnel_activity_refresh_clears_soft_prompted(...)
-async def test_adaptation_selection_entry_clears_soft_prompted(...)
```

Всі три тестували `handle_adaptation_flow` — функцію, яку видалено.

---

### `tests/test_plan_completion_metrics.py` — повна перезаміна

**Проблема:** тести були написані під стару версію `build_completion_metrics` що викликала `fetch_delivered_steps`. Після зміни на прямий `.join()` запит — `DummyQuery.join` → `AttributeError` (7 pre-existing failures).

**Виправлення:**

```diff
-@dataclass
-class DummyStep:
-    is_completed: bool = False
-    skipped: bool = False
+class DummyStep:
+    step_status: str = "pending"   # "completed" | "skipped" | "expired"
+    scheduled_for: datetime | None = None

-monkeypatch.setattr(metrics, "fetch_delivered_steps", lambda *_: delivered)
+monkeypatch.setattr(metrics, "_fetch_eligible_steps", lambda *_: eligible)
```

Дата-хелпер для streak тестів:

```python
# active_days default = MON-FRI
# Jan 5 (Mon) → Jan 6 (Tue): consecutive
# Jan 6 (Tue) → Jan 8 (Thu): Jan 7 (Wed) is active → streak breaks
_WEEKDAY_DTS = [None,
    datetime(2026, 1, 5, 12, 0, tzinfo=timezone.utc),  # 1 Mon
    datetime(2026, 1, 6, 12, 0, tzinfo=timezone.utc),  # 2 Tue
    ...
]
```

**Результат:** 8/8 pass (було 7 failures).

---

### `app/telemetry.py` — додаткове прибирання

```diff
-import asyncio

-ADAPTATION_EVENT_TYPES = {
-    "adaptation_proposed", "adaptation_rejected",
-    "adaptation_undo_requested", "adaptation_undo_blocked",
-}
-PROPOSAL_MESSAGES = { RuleEngine.PROPOSAL_REDUCE_LOAD: "...", ... }

-def _maybe_emit_system_prompt(db, user, window, event_type): ...  # 90 рядків
-    # — єдина функція що викликала RuleEngine і логувала adaptation_proposed

-async def _send_system_message_async(chat_id, text): ...
-def _dispatch_system_message(user, text): ...
# обидві orphaned після видалення _maybe_emit_system_prompt
```

Call site:

```diff
-    if event_type in {"task_skipped", "task_ignored", "task_failed"} and step_value:
-        db.flush()
-        _maybe_emit_system_prompt(db, user, window, event_type)
     return event
```

### `app/logic/rule_engine.py` — видалений повністю

Єдиний caller — `_maybe_emit_system_prompt`. Файл 37 рядків.

### `app/fsm/states.py` + `app/fsm/__init__.py`

```diff
-# Legacy stubs (T5.3)
-PLAN_FLOW_STATES: frozenset[str] = frozenset()
-PLAN_FLOW_ENTRYPOINTS: frozenset[str] = frozenset()
```

Стаби додавались як тимчасовий фікс P1 — видалені після того, як очищення orchestrator поставлено в план як окреме завдання.

---

### `tests/test_task_execution.py` — правка стейту

```diff
-    """User in ADAPTATION_FLOW → no state changes."""
-    user = DummyUser(tg_id=123, user_id=42, current_state="ADAPTATION_FLOW")
+    """User not in ACTIVE state → no state changes."""
+    user = DummyUser(tg_id=123, user_id=42, current_state="ACTIVE_PAUSED")
```

`ADAPTATION_FLOW` — видалений стан. Логіка тесту збережена.

---

### `tests/test_fsm_states.py` — новий regression suite

```python
REMOVED_ADAPTATION_STATES = [
    "ADAPTATION_SELECTION", "ADAPTATION_PARAMS",
    "ADAPTATION_CONFIRMATION", "ADAPTATION_FLOW",
]

def test_adaptation_states_not_valid_fsm():
    for state in REMOVED_ADAPTATION_STATES:
        assert is_valid_fsm_state(state) is False

def test_no_transition_into_adaptation_states():
    # can_transition("ACTIVE", "ADAPTATION_SELECTION") == False — і так далі

def test_plan_flow_states_not_valid_fsm(): ...
def test_pause_resume_transitions_still_work(): ...
def test_plan_creation_transitions_still_work(): ...
def test_onboarding_completion_transition_still_works(): ...
def test_schedule_adjustment_transitions_still_work(): ...
```

---

## Тести

```
tests/test_fsm_states.py               — 8 passed  ✅  (новий)
tests/test_plan_completion_metrics.py  — 8 passed  ✅  (було 7 failures)
tests/test_plan_pause_v5.py            — 9 passed  ✅
tests/test_plan_builder_v5.py          — 8 passed  ✅
Total: 33 passed in 0.88s
```

---

## Reviewer grep: результати

```
rg "ADAPTATION_|adaptation_executor|adaptation_preview|EXECUTE_ADAPTATION|adaptation_flow" app tests
```

**Результат після merge-ready стану:**

```
app/plan_adaptations.py — _ALLOWED_ADAPTATION_TYPES, _ADAPTATION_ALIASES
    → internal constants в pause/resume обробнику. Не routing, не flow.

tests/test_fsm_states.py — REMOVED_ADAPTATION_STATES list
    → regression-тест, навмисно перелічує видалені стани.
```

**Нуль runtime callers. Нуль adaptation routing.**

Перевірка по 5 компонентах:

| Компонент | Стан |
|-----------|------|
| FSM | Не знає adaptation states — `is_valid_fsm_state("ADAPTATION_SELECTION") == False` ✅ |
| orchestrator | Не веде adaptation flow — всі `handle_adaptation_*` функції видалені ✅ |
| ai_plans | Не пропонує adaptation tools — всі 3 dispatch гілки і tool definitions видалені ✅ |
| scheduler | Не чекає stuck adaptations — `check_stuck_adaptations` job видалений ✅ |
| telegram | Немає adaptation callbacks — `_ADAPTATION_ACTIONS`, `on_adaptation_action` видалені ✅ |

Пауза / resume / change time — залишаються як окремі runtime actions в `app/plan_pause.py` і `app/plan_adaptations.py` (execution-only, без flow, без FSM станів).

---

## Що залишилось (наступні PR)

| Завдання | Файли |
|----------|-------|
| Очистити мертвий PLAN_FLOW код з orchestrator.py | `app/orchestrator.py` (~25 блоків) |
| Міграція: drop adaptation schema | `app/db.py` + новий `.sql` файл |
| Видалити `had_adaptations`/`adaptation_count` з CompletionMetrics | `app/plan_completion/` |
